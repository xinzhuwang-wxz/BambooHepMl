# GPU-optimized requirements for BambooHepMl
# Use this file when building Docker images with GPU support

# Core ML frameworks (GPU versions)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# HEP data processing
uproot>=5.0.0
awkward>=2.0.0
numpy>=1.24.0

# ML engineering
mlflow>=2.0.0
tensorboard>=2.13.0
onnx>=1.14.0
onnxruntime-gpu>=1.15.0

# API and serving
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# Distributed training
ray[default]>=2.5.0
ray[train]>=2.5.0

# Utilities
pyyaml>=6.0
typer>=0.9.0
rich>=13.0.0

# Testing (optional, for development)
pytest>=7.4.0
pytest-cov>=4.1.0
