# Data configuration for incident energy regression.
# Target: IncidentEnergy (a scalar branch '_IncidentEnergy' in ROOT file).
# Shares the same EDM4hep features as classification.

treename: "events"

labels:
  type: complex
  value:
    _label_: "_IncidentEnergy"
